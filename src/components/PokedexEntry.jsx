import { useParams } from "react-router-dom";

export default function PokedexEntry() {
    const { id } = useParams();
    // getAdditionalInfo(pokemon);
    return (
        <li>

        </li>
    )
}
